import axios from "axios";

const tickerData = () => {
  try {
    // const results = await Promise.all(axios.get(API_ENDPOINTS.bitfinex));
  } catch {}
};

export default async function handler(req: Request, res: Response) {}
